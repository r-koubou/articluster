<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <LangVersion>{{language_version}}</LangVersion>
        <TargetFramework>{{target_framework}}</TargetFramework>
        <Nullable>enable</Nullable>
        <Copyright>{{author}}</Copyright>
        <Company>{{author}}</Company>
        <Product>{{namespace_prefix}}.{{project_name}}</Product>
        <AssemblyName>{{namespace_prefix}}.{{project_name}}</AssemblyName>
        <RootNamespace>{{namespace_prefix}}.{{project_name}}</RootNamespace>
    </PropertyGroup>

</Project>
